import TabBarText from '../TabBarText';

const StatisticsIconLabel = ({ focused }: { focused: boolean }) => (
  <TabBarText focused={focused} text="Statistics" />
);

export default StatisticsIconLabel;
